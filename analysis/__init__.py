"""TRACE-Kin packaging analysis scripts.

These produce the publication-ready deliverables called for in section 1A of
``trace_doc/TRACE_implementation_plan.md`` (clean CSV, tables, statistics,
v1-vs-v2 comparison, promotion gate). All inputs come from
``trace_doc/`` (immutable) or ``data/benchmark/`` (rebuild outputs).
"""
